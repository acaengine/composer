import { BindingDirective } from './binding.directive';

describe('BindingDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingDirective();
    expect(directive).toBeTruthy();
  });
});
